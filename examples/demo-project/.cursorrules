# 订单服务项目规则（demo）

## 硬性规则

- 金额必须用整数「分」，禁止用浮点。
- 错误必须用中文 ErrorXxx 抛出。
- 提交前必须运行 npm run lint。
- 严禁使用 any 类型。
- 日期格式统一 YYYY-MM-DD，禁止斜杠。
- 仓库禁止执行 git push --force。
- API 响应必须做异常兜底，不允许裸 throw。
- 注释一律使用中文。
